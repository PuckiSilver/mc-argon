name: mc-argon
author: PuckiSilver
version: v1.2.0
output: build
require: [bolt]

data_pack:
  load: src
  name: mc-argon
  description: Match the correct entity when hitting or being hit

pipeline: [mecha, plugins.credit]
meta:
  credit: [argon]
